apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Current live clusters have no Flux CRDs and never established Flux ownership of the
# retained legacy HelmRelease; future Flux use must keep the manual release outside this graph.
resources:
  - loki-values.yaml
  - loki.yaml
  - promtail-values.yaml
  - promtail.yaml
