apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# LEGACY/FUTURE ONLY: the Flux exporter manifest is deliberately not reconciled.
# Future Flux use must never share ownership with the manual Helm/Probe lifecycle.
resources:
  - loki-values.yaml
  - loki.yaml
  - promtail-values.yaml
  - promtail.yaml
