# Example values for optional production preview/canary deploys.
# Use this when validating at prod.democratized.space before/alongside apex promotion.
environment: prod

ingress:
  enabled: true
  className: traefik
  host: prod.democratized.space
