# Example base values for dspace deployments.
# This file is shared across dev/staging/prod values chains.
# Environment/topology details (for example node placement and HA) live in
# docs/apps/dspace.md and are applied via overlays, not encoded here.
environment: dev

ingress:
  enabled: true
  className: traefik
  annotations: {}
