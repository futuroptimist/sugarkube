# Base/shared token.place values consumed alongside environment overlays.
environment: dev
replicaCount: 1

image:
  repository: ghcr.io/futuroptimist/tokenplace-relay

ingress:
  enabled: true
  className: traefik
  annotations: {}

