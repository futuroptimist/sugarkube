# Example values for steady-state production (apex) deploys via Helm.
# The ingress host is democratized.space unless intentionally overridden.
environment: prod

ingress:
  enabled: true
  className: traefik
  host: democratized.space
