# ls-remote fixtures

Cached `git ls-remote --tags` outputs for GitHub actions referenced in the Pi image
workflow tests. These fixtures keep `tests/test_pi_image_tooling.py` self-contained and
avoid live network requests during CI.
