{
  "id": "2025-11-01-docs-verify-justfile-cli",
  "date": "2025-11-01",
  "component": "justfile docs recipes",
  "rootCause": "The docs-verify and simplify-docs Just recipes still used legacy templating spacing, so the enforcement tests did not detect the intended CLI invocation and the automation diverged from the Makefile target.",
  "resolution": "Updated the docs-verify and simplify-docs Just recipes to invoke the sugarkube CLI with the canonical \"{{sugarkube_cli}}\" template, restoring parity with Make.",
  "references": [
    "justfile",
    "tests/test_docs_verify_wrapper.py"
  ]
}
