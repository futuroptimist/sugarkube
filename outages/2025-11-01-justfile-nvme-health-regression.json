{
  "id": "2025-11-01-justfile-nvme-health-regression",
  "date": "2025-11-01",
  "component": "justfile nvme-health recipe",
  "rootCause": "A formatting change reintroduced spaces inside the moustache wrapper for the sugarkube CLI, so the guard in tests/test_nvme_health_workflow.py no longer detected the expected wrapper and failed.",
  "resolution": "Restored the recipe to invoke \"{{sugarkube_cli}}\" nvme health so the CLI wrapper guard passes and the workflow stays routed through the unified entrypoint.",
  "references": [
    "justfile",
    "tests/test_nvme_health_workflow.py"
  ]
}
