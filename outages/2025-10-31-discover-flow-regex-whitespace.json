{
  "id": "discover-flow-regex-whitespace",
  "date": "2025-10-31",
  "component": "tests/bats/discover_flow.bats",
  "rootCause": "The discover_flow suite asserted combined key/value log lines with an unescaped space inside a [[ =~ ]] regex, which Bash 5.2 now treats as a syntax error so the bats runner aborts before executing the file.",
  "resolution": "Escape the literal space in the regex so the check remains literal while keeping the concise =~ assertion.",
  "references": [
    "tests/bats/discover_flow.bats"
  ]
}
