{
  "id": "pi-image-node-runtime-missing",
  "date": "2025-10-13",
  "component": "pi-image GitHub workflow",
  "rootCause": "The disk cleanup step deleted /opt/hostedtoolcache, removing the Node.js runtime required by JavaScript actions such as actions/cache and actions/upload-artifact, causing the workflow to abort before collecting artifacts.",
  "resolution": "Adjusted the cleanup step to preserve the toolcache and added a dedicated node --version guard so future runs fail fast if Node disappears.",
  "references": [
    ".github/workflows/pi-image.yml",
    "tests/test_pi_image_tooling.py"
  ]
}
