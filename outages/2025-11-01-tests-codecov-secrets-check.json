{
  "id": "tests-codecov-secrets-check",
  "date": "2025-11-01",
  "component": "tests GitHub workflow",
  "rootCause": "tests.yml workflow conditional includes 'secrets.CODECOV_TOKEN != ''' which is invalid - GitHub Actions secrets cannot be checked for emptiness in conditionals. This causes the conditional to always evaluate incorrectly.",
  "resolution": "Removed the invalid secrets check from the upload coverage step conditional. The codecov-action will handle missing tokens gracefully by skipping upload.",
  "references": [
    ".github/workflows/tests.yml"
  ]
}
