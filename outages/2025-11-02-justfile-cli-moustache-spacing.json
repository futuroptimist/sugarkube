{
  "id": "justfile-cli-moustache-spacing",
  "date": "2025-11-02",
  "component": "automation.justfile",
  "rootCause": "Just recipes for docs, nvme-health, token-place, and start-here still referenced the CLI helper with spaced moustache expressions so the unified CLI wrapper string never matched the tests.",
  "resolution": "Tightened the moustache expressions so each recipe calls \"{{sugarkube_cli}}\" and forwards its argument variable without extra whitespace.",
  "references": [
    "tests/test_docs_verify_wrapper.py",
    "tests/test_nvme_health_workflow.py",
    "tests/test_start_here_command.py",
    "tests/test_token_place_wrappers.py",
    "justfile"
  ]
}
