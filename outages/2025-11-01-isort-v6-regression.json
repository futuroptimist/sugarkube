{
  "id": "2025-11-01-isort-v6-regression",
  "date": "2025-11-01",
  "component": "checks.sh lint pipeline",
  "rootCause": "pip started installing isort 6.0, whose new formatting rules fail the repo-wide import check even on untouched files.",
  "resolution": "Pinned checks.sh to install isort<6 and downgraded the tool in CI so import checks use the expected 5.x rules.",
  "references": [
    "checks.sh failure: ./scripts/checks.sh (chunk 78e9dd)",
    "pip downgrade log (chunk 3fcd30)"
  ]
}
