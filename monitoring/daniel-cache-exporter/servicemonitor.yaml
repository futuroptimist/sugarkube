apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: daniel-cache-exporter
  namespace: monitoring
spec:
  selector:
    matchLabels: {app.kubernetes.io/name: daniel-cache-exporter}
  endpoints:
    - port: metrics
      interval: 60s
      scrapeTimeout: 15s
